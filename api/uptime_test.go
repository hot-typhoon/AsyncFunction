package handler

import "testing"

func TestUptime(t *testing.T) {
	t.Run("uptime_Test", func(t *testing.T) {
		// _, err := true, error(nil)
		// if err != nil {
		// 	t.Errorf("Error: %v", err)
		// }
	})
}
